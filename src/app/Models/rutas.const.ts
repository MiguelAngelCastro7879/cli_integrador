import { environment } from 'src/environments/environment';

export const rutas={
    login:`${environment.urlbase}/login`,
    register:`${environment.urlbase}/user`,
    token_validacion:`${environment.urlbase}/get_user`,
    // login:`${environment.urlbase}/login`,
    // login:`${environment.urlbase}/login`,
    // login:`${environment.urlbase}/login`,
    // login:`${environment.urlbase}/login`,
    // login:`${environment.urlbase}/login`,
    
}